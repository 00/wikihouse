<%inherit file="library.tmpl"/>

<%def name="sub_title()">${_(u'Moderate')}</%def>

<%def name="content()">
  <h4 class="padding-sides">${_(u'Moderate')}</h4>
  <ul class="designs">
    % for item in designs:
      ${self.design_listing(item, show_series=True)}
    % endfor
  </ul>
</%def>